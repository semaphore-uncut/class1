version: v1.0
name: Staging deployment pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Deploy"
    task:
      secrets:
        - name: aws-stg-creds
      jobs:
      - name: "Deploy"
        commands:
          - env | grep "AWS_"
          - ls /home/semaphore/ec2_id_dsa
          - cat /home/semaphore/ec2_id_dsa
          - checkout
          - echo "deploying.... done"

  - name: "Smoke tests"
    task:
      jobs:
      - name: "Smoke test staging env"
        commands:
          - echo "all good"


promotions:
  - name: Production env
    pipeline_file: prod-deployment.yml
  - name: Beta env
    pipeline_file: beta.yml
