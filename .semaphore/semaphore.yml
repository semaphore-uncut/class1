version: v1.0
name: Build & test ppl
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build & Unit tests"
    task:
      jobs:
      - name: "Unit tests #1"
        commands:
          - checkout
          - ls -lah
          - pwd
          - echo "done"
      - name: "List"
            commands:
              - checkout
              - echo "Linting..."          
