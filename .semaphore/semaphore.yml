version: v1.0
name: Build & test ppl
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build & Unit tests"
    task:
      jobs:
      - name: "Unit tests #1"
        commands:
          - checkout
          - echo "done 1"
      - name: "Unit tests #2"
        commands:
          - checkout
          - echo "done 2"

  - name: "Ingration tests"
    task:
      jobs:
      - name: "Integ tests"
        commands:
          - checkout
          - echo "done with integ tests"
