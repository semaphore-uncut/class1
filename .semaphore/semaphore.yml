version: v1.0
name: Build & test ppl
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build & Unit tests"
    task:
      epilogue:
        commands:
          - echo "After job commands"
          - echo "Job result"
          - echo $SEMAPHORE_JOB_RESULT
      jobs:
      - name: "Unit tests #1"
        commands:
          - checkout
          - ls -lah
          - pwd
          - echo "done"
          
      - name: "Lint"
        commands:
          - checkout
          - echo "Linting..."
          - exit 1

  - name: "Integration tests"
    task:
      jobs:
      - name: "Integ tests"
        commands:
          - checkout
          - ls -lah
          - pwd
          - echo "done"       
