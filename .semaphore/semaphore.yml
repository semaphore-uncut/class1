version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      jobs:
      - name: "Job #1"
        commands:
          - checkout
          - ls -1
          - echo "done"

      - name: "Job #2"
        commands:
          - checkout
          - ls -1
          - echo "done"

  - name: "Second build step"
    task:
      jobs:
      - name: "Job #1"
        commands:
          - checkout
          - ls -1
          - echo "done"
